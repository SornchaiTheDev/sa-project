version: "3.8"
services:
  postgres:
    image: postgres:16
    container_name: postgres_container
    env_file:
      - ../.env
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  pgdata:
