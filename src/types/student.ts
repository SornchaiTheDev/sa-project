export interface Student {
  username: string;
  title: string;
  firstName: string;
  lastName: string;
  email: string;
  isActive: boolean;
  activityHours: JSON | null;
  phoneNumber: string | null;
  description: string | null;
  gpax: string | null;
}
